This folder contains unrelated legacy app scaffolding that is not part of the Think Green static landscaping template.

The live website and Netlify deploy use the top-level static HTML/CSS/JS files plus `netlify/functions/send-ticket-emails.js`.

Keep this folder out of client-facing template clones unless you intentionally need the old app stack.
